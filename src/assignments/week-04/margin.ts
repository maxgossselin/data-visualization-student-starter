// Margin reserves space around the plot for the title, axes, and axis labels.
// The concrete values live in AgeFamilySizePlot.tsx so they can be tweaked in one place.
export interface Margin {
  top: number;
  right: number;
  bottom: number;
  left: number;
}
